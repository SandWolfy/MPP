export interface IMagicItem {
    id: number
    name: string
    location: string
    usableClass: string
    price: number
}

export interface IBuff {
    bid: number
    mid: number
    name: string
    intensity: number
}
